{
  "name": "drift-city-portal",
  "version": "1.0.0",
  "description": "Drift City Digital Portal — The Asphalt Creed",
  "scripts": {
    "test": "jest --testEnvironment=jsdom --verbose",
    "test:coverage": "jest --testEnvironment=jsdom --coverage",
    "serve": "npx serve . -p 3000"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "testMatch": ["**/tests/**/*.test.js"]
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "jsdom": "^25.0.1"
  }
}
